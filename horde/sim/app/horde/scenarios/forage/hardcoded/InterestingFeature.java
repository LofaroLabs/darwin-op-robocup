package sim.app.horde.scenarios.forage.hardcoded;

public class InterestingFeature
    {
    public InterestingFeature(Class<?> f, Class<?> t)
        {
        feature = f;
        target = t;
        }

    public Class<?> feature;
    public Class<?> target;
    };
